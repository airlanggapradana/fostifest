{
  "projects": {
    "client": {
      "rootDirectory": "client",
      "buildCommand": "npm run build",
      "outputDirectory": "dist"
    },
    "server": {
      "rootDirectory": "server",
      "buildCommand": "npm run build",
      "outputDirectory": "dist"
    }
  }
}
